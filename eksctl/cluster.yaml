# doc: https://schema.eksctl.io
apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: test-cluster
  region: us-east-1
  version: "1.36"
nodeGroups:
  - name: ng1
    instanceType: t3.small
    desiredCapacity: 2
    minSize: 2
    maxSize: 2
    volumeSize: 20
    ssh:
      publicKeyName: macbook

apiVersion: v1
kind: Service
metadata:
  name: java-maven-app-svc
spec:
  type: ClusterIP
  selector:
    app: java-maven-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 32080